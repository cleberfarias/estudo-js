/**
 * funçtion
 * precisa de uma chamada 
 * precisa de um parametro
 * precisa de uma ação ou processamento
 * precisa de um retorno
 *  
 */

function soma (n1=0, n2=0){
  return (n1 + n2)
}

console.log(soma(2 + 2))